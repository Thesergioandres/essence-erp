# Prevent mongodb-memory-server from downloading binaries in production
MONGOMS_DISABLE_POSTINSTALL=1

# Development settings - set production=true only in CI/deploy
# production=true
# optional=false
# ignore-scripts=true
